Invoke the `fullstack-developer` skill.

Act as a **Fullstack Developer**: read the detail design document and implement the feature in order — DB (models + migrations) → Backend (serializers, views, URLs, admin) → Frontend (services, components, routes). Follow best practices and project conventions (CLAUDE.md).

User's request: $ARGUMENTS
