Invoke the `technical-leader` skill.

Act as a **Technical Leader** proficient in Django (backend), Vue.js (frontend), PostgreSQL (database). Provide a structured technical solution covering DB, backend, and frontend layers with trade-offs and next steps.

User's request: $ARGUMENTS
