import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Gold — matches web --accent-gold: #D69E2E
  static const primaryGold = Color(0xFFD69E2E);
  // Lighter gold — matches web --accent-golden: #ECC94B
  static const accentGolden = Color(0xFFECC94B);

  // Backgrounds — matches web --bg-page-dark: #111C21
  static const background = Color(0xFF111C21);
  // Card surface — matches web --bg-card: #1A262D
  static const surface = Color(0xFF1A262D);
  // Input background — matches web --bg-input: #0D1619
  static const surfaceAlt = Color(0xFF0D1619);

  // Text — matches web --text-primary: #FFFFFF
  static const textPrimary = Color(0xFFFFFFFF);
  // Text secondary — matches web --text-secondary: #F0F0F0
  static const textSecondary = Color(0xFFF0F0F0);
  // Muted text for icons/placeholders
  static const textMuted = Color(0xFF9E9E9E);

  static const lockGray = Color(0xFF616161);
  static const demoBadge = Color(0xFFFFC107);
  static const error = Color(0xFFCF6679);
  static const success = Color(0xFF4CAF50);
  // Primary blue — matches web --btn-primary: #1481B8
  static const primaryBlue = Color(0xFF1481B8);
}
